<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
</div>

{{#if post.comments}}


        <h3>Comments</h3>

      <div class="card-body">
        {{#each post.comments}}
          <div>
            <p>
              {{comment_body}}
            
              &mdash; {{user.name}}
            </p>
          </div>
        {{/each}}
      </div>
  
  {{/if}}

  <form id="new-comment-form">
      <h3>Create a Comment</h3>
    <div class="card-body">
      <input type="hidden" name="post-id" value="{{post.id}}" />
      <textarea name="comment-body" placeholder="Comment..." class="form-input"></textarea>
      <button type="submit" class="btn">Submit</button>
    </div>
  </form>
</div>

<script src="/js/comment.js"></script>